apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: target-system
spec:
  selector:
    name: target-system-frontend-deploy
  ports:
  - port: 5000
    targetPort: 5000
  type: ClusterIP
