apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: target-system
spec:
  selector:
    name: target-system-backend-deploy
  ports:
  - port: 5000
    targetPort: 5000
  type: ClusterIP
